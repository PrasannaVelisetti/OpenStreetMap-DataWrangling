process.py file uses the .osm file as input and scans through each element to extract the
user information, address, type of the element, positional coordinates and other information
and export it as a .json file.

audit.py file is used for auditing  postal codes and phone numbers. Regex expressions are used 
to validate the data.